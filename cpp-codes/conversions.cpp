#include <bits/stdc++.h>
using namespace std;

int32_t main() {
    string num_str;
    int num_int = stoi(str_num);
    float num_float = stof(str_num);
    double num_double = stod(str_num);

    string num_str_2 = to_string(num_int);
}
